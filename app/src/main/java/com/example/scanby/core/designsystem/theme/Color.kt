package com.example.scanby.core.designsystem.theme

import androidx.compose.ui.graphics.Color

object ScanbyColor {
    val Accent = Color(0xFF16A18C)
    val AccentPressed = Color(0xFF0E7A6B)
    val AccentGradientEnd = Color(0xFF007964)

    val Navy = Color(0xFF0B1E23)
    val Ink = Color(0xFF16232A)
    val Paper = Color(0xFFF5F8F8)

    val CaptionSub = Ink.copy(alpha = 0.58f)
    val StepDotInactive = Navy.copy(alpha = 0.14f)
    val HeaderDivider = Navy.copy(alpha = 0.07f)
    val OnNavySecondary = Color.White.copy(alpha = 0.7f)
}
