package com.example.scanby.navigation

/**
 * Route name used to register/navigate to the onboarding flow in the app's NavHost.
 * More route objects will land here (e.g. LoginRoute, HomeRoute) as those features
 * get built — this package exists so features never navigate to each other directly.
 */
object OnboardingRoute {
    const val route = "onboarding"
}
